269a270,289
>   public boolean isStruct() {
>     for (JSType typ : getAlternates()) {
>       if (typ.isStruct()) {
>         return true;
>       }
>     }
>     return false;
>   }
> 
>   @Override
>   public boolean isDict() {
>     for (JSType typ : getAlternates()) {
>       if (typ.isDict()) {
>         return true;
>       }
>     }
>     return false;
>   }
> 
>   @Override
