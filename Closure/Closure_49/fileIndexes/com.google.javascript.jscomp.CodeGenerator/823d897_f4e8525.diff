746,748c746,752
<       long l = Long.parseLong(s);
<       if (l < NodeUtil.MAX_POSITIVE_INTEGER_NUMBER) {
<         return l;
---
>       try {
>         long l = Long.parseLong(s);
>         if (l < NodeUtil.MAX_POSITIVE_INTEGER_NUMBER) {
>           return l;
>         }
>       } catch (NumberFormatException e) {
>         
