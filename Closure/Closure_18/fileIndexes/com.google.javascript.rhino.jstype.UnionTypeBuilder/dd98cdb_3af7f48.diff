158,160c158
<     if (isAllType) {
<       return registry.getNativeType(ALL_TYPE);
<     } else if (isNativeUnknownType) {
---
>     if (isNativeUnknownType) {
165a164,165
>     } else if (isAllType) {
>       return registry.getNativeType(ALL_TYPE);
