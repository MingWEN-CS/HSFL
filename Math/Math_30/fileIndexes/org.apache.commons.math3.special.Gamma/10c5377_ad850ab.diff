41c41
<     private static final double[] lanczos =
---
>     private static final double[] LANCZOS =
97,98c97,98
<             for (int i = lanczos.length - 1; i > 0; --i) {
<                 sum = sum + (lanczos[i] / (x + i));
---
>             for (int i = LANCZOS.length - 1; i > 0; --i) {
>                 sum = sum + (LANCZOS[i] / (x + i));
100c100
<             sum = sum + lanczos[0];
---
>             sum = sum + LANCZOS[0];
