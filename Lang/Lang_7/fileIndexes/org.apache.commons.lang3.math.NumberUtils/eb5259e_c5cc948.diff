443a444
> 
458a460,466
>             int hexDigits = str.length() - 2; 
>             if (str.startsWith("-")) { 
>                 hexDigits--;
>             }
>             if (hexDigits > 8) { 
>                 return createLong(str);
>             }
