1639a1640,1642
>             int limit = text.length() - position;
> 
>             zeroOffset:
1640a1644,1654
>                 if (iZeroOffsetText.length() == 0) {
>                     
>                     if (limit > 0) {
>                         char c = text.charAt(position);
>                         if (c == '-' || c == '+') {
>                             break zeroOffset;
>                         }
>                     }
>                     bucket.setOffset(0);
>                     return position;
>                 }
1650d1663
<             int limit = text.length() - position;
